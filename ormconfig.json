{
  "name": "default",
  "type": "mongodb",
  "host": "localhost",
  "port": 30003,
  "database": "demo",
  "synchronize": true,
  "logging": false,
  "autoSchemaSync": true,
  "entities": ["src/entity/*.ts"],
  "migrations": ["src/migration/*.ts"],
  "cli": {
    "entitiesDir": "src/entity",
    "migrationsDir": "src/migration"
  }
}
